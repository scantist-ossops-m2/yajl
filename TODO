* Fix up all the copyright notices!
* add a test for 0x1F bug
* testing:
  - see http://seriot.ch/parsing_json.php (and https://github.com/nst/JSONTestSuite)
  a. the permuter
  b. some performance comparison against json_checker.
* investigate pull instead of push parsing
* set up example/* (parse_config) to also run a whole a test suite
